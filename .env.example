# HTTP port for Express + Socket.IO (default 3000)
PORT=3000

# Bind address: use 0.0.0.0 to listen on all interfaces (recommended for Docker and LAN)
HOST=0.0.0.0

# Optional: restrict CORS for Socket.IO. Comma-separated origins, or omit for "*"
# CORS_ORIGIN=https://your-app.example.com
